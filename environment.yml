name: misa-code-py39
channel_priority: strict        # faster, deterministic solver
channels:
  - pytorch                     # has the 1.11 / CUDA 11.3 binaries
  - conda-forge
  - defaults
  - intel
dependencies:
  # ── core interpreter ─────────────────────────────────────────────
  - python            =3.9
  - pip
  - wheel

  # ── numeric & science stack ──────────────────────────────────────
  - numpy             =1.26.*     # last NumPy to support 3.9
  - scipy             =1.12.*
  - pandas            =2.2.*
  - scikit-learn      =1.4.*
  - scikit-image      =0.22.*     # 0.23+ needs Py ≥ 3.10
  - matplotlib        =3.9.*
  - pillow
  - cython

  # ── PyTorch + CUDA 11.3 (GPU) ────────────────────────────────────
  - cudatoolkit       =11.3       # comment out for CPU runtime
  - pytorch           =1.11.*
  - torchvision       =0.12.*
  - torchaudio        =0.11.*     # conda build for Torch 1.11

  # ── NLP stack ────────────────────────────────────────────────────
  - spacy             =3.7.*

  # ── runtime libs (un-pinned builds) ─────────────────────────────
  - openssl
  - libffi
  - zlib
  - sqlite
  - ncurses
  - readline
  -ittapi

  # ── pip-only packages ───────────────────────────────────────────
  - pip:
      # core research libs
      - allennlp==2.10.*
      - allennlp-models==2.10.*
      - transformers==4.20.*

      # (torch-geometric & friends come later—see below)

      # utility & notebook stack
      - jupyter
      - notebook
      - ipykernel
      - ipython
      - seaborn
      - requests>=2.31
      - tqdm>=4.67

      # keep the rest of your pure-Python helpers as needed …
      - absl-py
      - argparse
      - astor
      - bleach
      - boto3
      - cachetools
      - click
      - decorator
      - docopt
      - ftfy>=5.8
      - gensim
      - grpcio
      - imageio
      - importlib-metadata
      - joblib>=1.4
      - networkx>=2.8
      - nltk>=3.8
      - packaging>=25
      - regex
      - sacremoses
      - smart-open
      - tensorboard
      - tensorboardx
      - termcolor
      - tornado
      - traitlets
      - unidecode
      - urllib3>=2.2
      - validators
      - wrapt
      - zipp

